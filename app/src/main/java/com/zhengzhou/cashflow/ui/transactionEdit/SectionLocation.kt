package com.zhengzhou.cashflow.ui.transactionEdit

import androidx.compose.runtime.*
import androidx.compose.ui.res.stringResource

/*
@Composable
fun LocationSection(
    uiState: OperationViewModel.UiState,
    viewModel: OperationViewModel,
) {
    var ifLocationSelected by remember {
        mutableStateOf(
            !uiState.location.equal(TagLocation())
        )
    }

    // TODO: to  implement

}

@Composable
private fun slotText(
    ifSelected: Boolean,
    uiState: OperationViewModel.UiState
): String {
    return if (ifSelected) {
        uiState.location.address
    } else {
        stringResource(id = R.string.location_add)
    }
}

 */